const int max=100;
float main()
{
	int i,j,k;
	int rem;   // rem:remainder
	float result = 1.0;

	int i=2;
	while(i<=max) {
		prime = 1.5;
		i/2 = k;
		j=2;
		while(j<=k){
			rem=i%j;
			if(rem==0) prime=0; 
			++j;
		}
		if(prime==1) write(i); 
		++i
	return result;
}